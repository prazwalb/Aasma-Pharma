// LIST of Enums
// they cannot be vreated inside class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivery }

enum PaymentMethods { esewa, khalti, fonePay, cod }
